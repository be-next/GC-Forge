[package]
name = "gc-forge-runner"
description = "JVM orchestration for GC-Forge (Docker MVP, native runner V1)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true

[dependencies]
anyhow    = { workspace = true }
thiserror = { workspace = true }
tracing   = { workspace = true }
gc-forge-scenario = { workspace = true }

[dev-dependencies]
tempfile = "3.13"

[features]
# When enabled, the integration tests that actually launch Docker run.
# Off by default so contributors without Docker still get a green
# `cargo test`. CI flips this on.
docker-integration = []

[lints]
workspace = true
